#!/home/odroid/jseo/jseo/jseo
collection [filter]
{
  var el = eval(each(this));
  if (el) jseo.list.add(el);
},

module [link] {<a href="${code}"> ${name} </a>  ${jseo.collection.add('jseo.link["'+name+'"]') ? "" : ""} },

link [one]{http://one.entry.repl.co/},
link [two]{http://two.entry.repl.co/},
link [three] {javascript: alert('here');},
link [four] {javascript: alert('collection');}

loader [list] { 
  <li> ${entry} </li> 
}, list ("one", "two"),

"hello", "world" [Test]{ Test += each + this;}
{

  jseo.commandLine.forEach([],
   function(each){
     jseo.list.add(this);
   }
  );

  jseo.collection.filter( (el) =>{
    if (el==jseo.collection.first)
	return "";
    if (el==jseo.collection.next)
	return "";
    return el;
  } );

  jseo.add(jseo.list);

  return this.Test(" :: ");

}
